{
  "name": "@adammcarter/use-cases",
  "version": "0.7.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adammcarter/use-cases.git"
  },
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b packages/core packages/cli packages/mcp && node packages/core/scripts/copy-schemas.mjs && chmod +x packages/cli/dist/index.js packages/mcp/dist/index.js && node scripts/bundle.mjs",
    "clean": "rm -rf packages/*/dist coverage",
    "cli": "node packages/cli/dist/index.js",
    "lint": "tsc -b --pretty false --dry",
    "test": "vitest run",
    "typecheck": "tsc -b --pretty false",
    "bundle": "node scripts/bundle.mjs"
  },
  "devDependencies": {
    "@types/node": "26.1.0",
    "esbuild": "0.28.2",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "yaml": "^2.9.0"
  },
  "private": true,
  "exports": {
    ".": "./opencode/plugin.js"
  }
}
