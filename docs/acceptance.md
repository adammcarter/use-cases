# Acceptance Matrix

This repo dogfoods presentation-skills through `use-cases/`.

The rows cover matrix loading, evidence recording, live showcase sign-off,
generated-plan proof binding, host projection conformance, MCP stdio parity,
TEST-MATRIX migration, and installable package checks. They are intended behavior
rows, not proof. Proof remains in append-only ledgers or command output.
