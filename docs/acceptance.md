# Acceptance Matrix

This repo dogfoods presentation-skills through `use-cases/`.

The rows cover matrix loading, evidence recording, live showcase sign-off, host
projection conformance, MCP parity, and TEST-MATRIX migration. They are intended
behavior rows, not proof. Proof remains in append-only ledgers or command output.
